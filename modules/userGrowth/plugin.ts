import Module from '../../core/bot/lib/structures/Module';
import { KlasaClient } from 'klasa';

export default class implements Module {
	setup(this: KlasaClient) {
		// In case you need to add more stuff on the client
	}
}
